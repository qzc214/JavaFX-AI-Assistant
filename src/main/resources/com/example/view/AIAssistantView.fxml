<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<VBox xmlns="http://javafx.com/javafx/21"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.example.AIController"
      spacing="15"
      style="-fx-padding: 20; -fx-background-color: linear-gradient(to bottom right, #667eea, #764ba2);">

    <!-- 标题区域 -->
    <HBox alignment="CENTER_LEFT" spacing="10">
        <Label fx:id="titleLabel" text="🤖 JavaFX AI 智能控制台"
               style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: white;
                      -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 8, 0, 0, 2);"/>

        <Region HBox.hgrow="ALWAYS"/>

        <HBox spacing="8" alignment="CENTER_RIGHT">
            <Label fx:id="statusLabel" text="正在初始化..."
                   style="-fx-font-size: 14px; -fx-text-fill: #f39c12; -fx-font-weight: bold;
                          -fx-padding: 5 10; -fx-background-color: rgba(255,255,255,0.1);
                          -fx-background-radius: 10;"/>

            <Button text="➕ 注册组件"
                    onAction="#registerNewComponent"
                    style="-fx-background-color: rgba(52, 152, 219, 0.8); -fx-text-fill: white;
                           -fx-font-weight: bold; -fx-background-radius: 8; -fx-padding: 6 12;"
                    HBox.hgrow="NEVER"/>
        </HBox>
    </HBox>

    <Separator style="-fx-background-color: rgba(255,255,255,0.3); -fx-padding: 5 0;"/>

    <!-- 主内容区域 -->
    <HBox spacing="20" VBox.vgrow="ALWAYS">
        <!-- 左侧：AI对话区 -->
        <VBox spacing="10" prefWidth="500" HBox.hgrow="ALWAYS">
            <Label text="💭 AI 对话区"
                   style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: white;"/>

            <TextArea fx:id="chatArea"
                      editable="false"
                      wrapText="true"
                      style="-fx-font-family: 'Microsoft YaHei', 'Segoe UI'; -fx-font-size: 14px;
                             -fx-control-inner-background: rgba(255,255,255,0.95);
                             -fx-background-radius: 12; -fx-border-radius: 12;
                             -fx-border-color: rgba(255,255,255,0.4); -fx-border-width: 2;"
                      VBox.vgrow="ALWAYS"/>

            <Label text="📝 对话历史"
                   style="-fx-font-size: 12px; -fx-text-fill: rgba(255,255,255,0.8);"/>
        </VBox>

        <!-- 右侧：控制面板 -->
        <VBox spacing="10" prefWidth="400" HBox.hgrow="ALWAYS">
            <Label text="🎛️ 控制面板"
                   style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: white;"/>

            <VBox fx:id="controlPanel"
                  spacing="12"
                  style="-fx-padding: 20; -fx-background-color: rgba(255,255,255,0.97);
                         -fx-background-radius: 15; -fx-border-radius: 15;
                         -fx-border-color: rgba(255,255,255,0.6); -fx-border-width: 2;"
                  VBox.vgrow="ALWAYS">

                <!-- 按钮控制组 -->
                <Label text="🔘 按钮控制"
                       style="-fx-font-weight: bold; -fx-font-size: 16px; -fx-text-fill: #2c3e50;"/>
                <HBox spacing="15" alignment="CENTER">
                    <Button fx:id="btn1" text="示例按钮 1"
                            style="-fx-background-color: linear-gradient(to right, #3498db, #2980b9);
                                   -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 14px;
                                   -fx-background-radius: 8; -fx-padding: 10 20; -fx-cursor: hand;"/>
                    <Button fx:id="btn2" text="示例按钮 2"
                            style="-fx-background-color: linear-gradient(to right, #9b59b6, #8e44ad);
                                   -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 14px;
                                   -fx-background-radius: 8; -fx-padding: 10 20; -fx-cursor: hand;"/>
                </HBox>

                <Separator/>

                <!-- 文本输入区 -->
                <Label text="📝 文本输入"
                       style="-fx-font-weight: bold; -fx-font-size: 16px; -fx-text-fill: #2c3e50;"/>
                <TextField fx:id="sampleText"
                           promptText="在这里输入任意文本..."
                           style="-fx-background-radius: 8; -fx-border-radius: 8;
                                  -fx-border-color: #bdc3c7; -fx-padding: 10; -fx-font-size: 14px;"/>

                <Separator/>

                <!-- 颜色选择器 -->
                <Label text="🎨 颜色选择"
                       style="-fx-font-weight: bold; -fx-font-size: 16px; -fx-text-fill: #2c3e50;"/>
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <Label text="选择颜色:"
                           style="-fx-font-size: 14px; -fx-text-fill: #7f8c8d;"/>
                    <ColorPicker fx:id="colorPicker"
                                 style="-fx-background-radius: 6;"/>
                </HBox>

                <!-- 预设颜色按钮 -->
                <HBox spacing="8" alignment="CENTER">
                    <Button text="🔴" userData="#FF0000" onAction="#applyColorPreset"
                            style="-fx-background-color: #FF0000; -fx-text-fill: white;
                   -fx-font-size: 12px; -fx-min-width: 30; -fx-min-height: 30;
                   -fx-background-radius: 15;"/>
                    <Button text="🟢" userData="#00FF00" onAction="#applyColorPreset"
                            style="-fx-background-color: #00FF00; -fx-text-fill: black;
                   -fx-font-size: 12px; -fx-min-width: 30; -fx-min-height: 30;
                   -fx-background-radius: 15;"/>
                    <Button text="🔵" userData="#0000FF" onAction="#applyColorPreset"
                            style="-fx-background-color: #0000FF; -fx-text-fill: white;
                   -fx-font-size: 12px; -fx-min-width: 30; -fx-min-height: 30;
                   -fx-background-radius: 15;"/>
                    <Button text="🟡" userData="#FFFF00" onAction="#applyColorPreset"
                            style="-fx-background-color: #FFFF00; -fx-text-fill: black;
                   -fx-font-size: 12px; -fx-min-width: 30; -fx-min-height: 30;
                   -fx-background-radius: 15;"/>
                    <Button text="🟣" userData="#800080" onAction="#applyColorPreset"
                            style="-fx-background-color: #800080; -fx-text-fill: white;
                   -fx-font-size: 12px; -fx-min-width: 30; -fx-min-height: 30;
                   -fx-background-radius: 15;"/>
                    <Button text="⚫" userData="#000000" onAction="#applyColorPreset"
                            style="-fx-background-color: #000000; -fx-text-fill: white;
                   -fx-font-size: 12px; -fx-min-width: 30; -fx-min-height: 30;
                   -fx-background-radius: 15;"/>
                </HBox>

                <!-- 颜色历史记录 -->
                <HBox spacing="5" alignment="CENTER_LEFT">
                    <Label text="最近使用:"
                           style="-fx-font-size: 13px; -fx-text-fill: #7f8c8d;"/>
                    <HBox fx:id="colorHistoryBox" spacing="5"/>
                </HBox>

                <Separator/>

                <Separator/>

                <!-- 系统信息 -->
                <Label text="ℹ️ 系统信息"
                       style="-fx-font-weight: bold; -fx-font-size: 16px; -fx-text-fill: #2c3e50;"/>
                <TextArea text="📌 使用说明：&#10;1. 在下方输入框输入自然语言指令&#10;2. AI 将解析并执行对应的操作&#10;3. 可控制的组件包括：按钮、文本框、标签等&#10;4. 示例指令：'隐藏按钮1'、'将标题改为蓝色'"
                          editable="false" wrapText="true"
                          style="-fx-background-color: rgba(236, 240, 241, 0.5); -fx-border-color: #ecf0f1;
                                 -fx-background-radius: 8; -fx-border-radius: 8; -fx-padding: 10;
                                 -fx-font-size: 13px; -fx-text-fill: #34495e;"/>
            </VBox>

            <Label text="👆 以上组件可通过自然语言控制"
                   style="-fx-font-size: 12px; -fx-text-fill: rgba(255,255,255,0.8);"/>
        </VBox>
    </HBox>

    <Separator style="-fx-background-color: rgba(255,255,255,0.3); -fx-padding: 5 0;"/>

    <!-- 指令输入区域 -->
    <VBox spacing="8">
        <Label text="💬 输入指令"
               style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: white;"/>

        <HBox spacing="12" alignment="CENTER">
            <TextField fx:id="commandInput"
                       HBox.hgrow="ALWAYS"
                       promptText="输入自然语言指令，如：'隐藏按钮1'、'将标题改为红色'、'显示所有组件'..."
                       style="-fx-background-radius: 25; -fx-border-radius: 25;
                              -fx-padding: 14 20; -fx-font-size: 15px;
                              -fx-border-color: rgba(255,255,255,0.4); -fx-border-width: 2;"/>

            <Button fx:id="executeButton"
                    text="🚀 执行指令"
                    style="-fx-background-color: linear-gradient(to right, #FF416C, #FF4B2B);
                           -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 16px;
                           -fx-background-radius: 25; -fx-padding: 14 30; -fx-cursor: hand;
                           -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 10, 0, 0, 2);"/>
        </HBox>

        <HBox spacing="15" alignment="CENTER">
            <Label text="💡 示例指令："
                   style="-fx-font-size: 13px; -fx-text-fill: rgba(255,255,255,0.9);"/>
            <Button text="隐藏按钮1"
                    onAction="#executeNaturalLanguageCommand"
                    style="-fx-background-color: rgba(255,255,255,0.15); -fx-text-fill: white;
                           -fx-font-size: 12px; -fx-background-radius: 15; -fx-padding: 4 10;"
                    userData="隐藏按钮1"/>
            <Button text="标题改蓝色"
                    onAction="#executeNaturalLanguageCommand"
                    style="-fx-background-color: rgba(255,255,255,0.15); -fx-text-fill: white;
                           -fx-font-size: 12px; -fx-background-radius: 15; -fx-padding: 4 10;"
                    userData="将标题改为蓝色"/>
            <Button text="显示所有组件"
                    onAction="#executeNaturalLanguageCommand"
                    style="-fx-background-color: rgba(255,255,255,0.15); -fx-text-fill: white;
                           -fx-font-size: 12px; -fx-background-radius: 15; -fx-padding: 4 10;"
                    userData="显示所有组件"/>
            <Button text="修改文本"
                    onAction="#executeNaturalLanguageCommand"
                    style="-fx-background-color: rgba(255,255,255,0.15); -fx-text-fill: white;
                           -fx-font-size: 12px; -fx-background-radius: 15; -fx-padding: 4 10;"
                    userData="将文本框内容改为Hello AI"/>
            <!-- 在示例指令区域添加 -->
            <Button text="设置颜色选择器"
                    onAction="#executeNaturalLanguageCommand"
                    style="-fx-background-color: rgba(255,255,255,0.15); -fx-text-fill: white;
               -fx-font-size: 12px; -fx-background-radius: 15; -fx-padding: 4 10;"
                    userData="将颜色选择器设置为紫色"/>
        </HBox>
    </VBox>

</VBox>